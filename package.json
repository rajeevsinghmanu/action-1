{
  "name": "action-1",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "mocha --reporter mocha-junit-reporter --reporter-options mochaFile=test-result.xml"
  },
  "devDependencies": {
    "mocha": "^11.0.0",
    "mocha-junit-reporter": "^2.2.1"
  }
}
